name: Production deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      force_native_build:
        description: Force a native build + store submit instead of OTA update
        type: boolean
        default: false

concurrency:
  group: production-deploy
  cancel-in-progress: true

jobs:
  cancel-previous:
    name: Cancel previous EAS builds
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - name: Cancel queued and in-progress EAS builds
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          for STATUS in new in-progress; do
            npx eas-cli build:list --profile production --platform ios --status "$STATUS" --json --non-interactive > /tmp/builds.json 2>/dev/null || true
            for BUILD_ID in $(jq -r '.[].id' /tmp/builds.json 2>/dev/null); do
              echo "Cancelling $STATUS build $BUILD_ID"
              npx eas-cli build:cancel "$BUILD_ID" --non-interactive
            done
          done

  setup:
    name: Install dependencies
    needs: cancel-previous
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - name: Install dependencies
        run: yarn install

  typecheck:
    name: Run TypeScript typecheck
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - name: Run tsc
        run: yarn tsc

  test:
    name: Run unit tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - name: Run unit tests
        run: yarn jest --coverage=false --detectOpenHandles --forceExit

  detect-fingerprint:
    name: Check if native build needed
    needs: [typecheck, test]
    runs-on: ubuntu-latest
    outputs:
      needs_native_build: ${{ steps.result.outputs.needs_native_build }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - name: Generate fingerprint
        id: fingerprint
        env:
          APP_ENV: production
        run: |
          IOS_HASH=$(npx @expo/fingerprint fingerprint:generate --platform ios | jq -r '.hash')
          echo "ios_hash=$IOS_HASH" >> $GITHUB_OUTPUT
      - name: Check for existing builds
        id: check
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          set +e
          npx eas-cli build:list \
            --fingerprint-hash ${{ steps.fingerprint.outputs.ios_hash }} \
            --build-profile production \
            --platform ios --status finished --limit 1 \
            --json --non-interactive > /tmp/builds.json 2>/dev/null
          IOS_BUILDS=$(jq 'length' /tmp/builds.json 2>/dev/null)
          IOS_BUILDS=${IOS_BUILDS:-0}
          if [[ "$IOS_BUILDS" -eq 0 ]]; then
            echo "needs_native_build=true" >> $GITHUB_OUTPUT
          else
            echo "needs_native_build=false" >> $GITHUB_OUTPUT
          fi
      - name: Apply force override
        id: result
        run: |
          if [[ "${{ inputs.force_native_build }}" == "true" ]]; then
            echo "needs_native_build=true" >> $GITHUB_OUTPUT
          else
            echo "needs_native_build=${{ steps.check.outputs.needs_native_build }}" >> $GITHUB_OUTPUT
          fi

  deploy-ota:
    name: Deploy OTA update
    needs: detect-fingerprint
    if: needs.detect-fingerprint.outputs.needs_native_build == 'false'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - name: Deploy OTA update
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: npx eas-cli update --branch production --non-interactive
      - name: Notify Rollbar of deploy
        run: |
          curl -s https://api.rollbar.com/api/1/deploy \
            -F access_token=${{ secrets.ROLLBAR_ACCESS_TOKEN }} \
            -F environment=production \
            -F revision=${{ github.sha }} \
            -F local_username=${{ github.actor }}

  build-and-submit:
    name: Build and submit to stores
    needs: detect-fingerprint
    if: needs.detect-fingerprint.outputs.needs_native_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - uses: actions/cache@v4
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
      - name: Bump app version
        run: |
          CURRENT=$(node -p "require('./app.config.js').expo.version")
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
          NEW_VERSION="$MAJOR.$MINOR.$((PATCH + 1))"
          sed -i "s/version: '$CURRENT'/version: '$NEW_VERSION'/" app.config.js
          echo "Bumped version $CURRENT → $NEW_VERSION"
      - name: Build for all platforms
        id: build
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          npx eas-cli build --profile production --platform ios --json --non-interactive > /tmp/build.json
          BUILD_ID=$(jq -r '.[0].id' /tmp/build.json)
          echo "build_id=$BUILD_ID" >> $GITHUB_OUTPUT
      - name: Submit to App Store
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EXPO_APPLE_ID: ${{ secrets.EXPO_APPLE_ID }}
          ASC_APP_ID: ${{ secrets.ASC_APP_ID }}
          EXPO_APPLE_TEAM_ID: ${{ secrets.EXPO_APPLE_TEAM_ID }}
        run: npx eas-cli submit --profile production --platform ios --id ${{ steps.build.outputs.build_id }} --non-interactive
      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add app.config.js
          git commit -m "chore: bump app version to $(node -p "require('./app.config.js').expo.version") [skip ci]"
          git push
      - name: Notify Rollbar of deploy
        run: |
          curl -s https://api.rollbar.com/api/1/deploy \
            -F access_token=${{ secrets.ROLLBAR_ACCESS_TOKEN }} \
            -F environment=production \
            -F revision=${{ github.sha }} \
            -F local_username=${{ github.actor }}
